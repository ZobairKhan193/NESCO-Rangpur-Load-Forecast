# App theme — matches the "NESCO Load Forecast Dashboard" design
# (dark navy UI, orange accent, purple action button, IBM Plex fonts via CSS in app.py).
[theme]
base = "dark"
primaryColor = "#7c3aed"
backgroundColor = "#0b1120"
secondaryBackgroundColor = "#0f1b30"
textColor = "#e2e8f0"
